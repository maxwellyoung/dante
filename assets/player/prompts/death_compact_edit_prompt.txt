Intended use: candidate production spritesheet for a 2D side-view platformer death animation review. Edit the provided transparent reference-canvas image into a single horizontal 4-frame death spritesheet. Use the supplied sprite as the exact design anchor: same character identity, palette, proportions, and readable face.
Composition: keep the image transparent, keep exactly one row of 4 equal 256x256 frame slots laid out left to right across the 1024x1024 canvas, centered vertically, with no overlap, no labels, and no extra objects.
Action: frame 1 stays as the compact idle-ready stance. Frames 2 through 4 should show a one-way collapse: hit shock, knees buckling into a compact drop, then a final grounded defeated pose. Keep frame 2 upright enough to remain inside the same silhouette family before the final collapse.
Scale target: keep the upright body compact enough to normalize to roughly 36 to 42 pixels of body height inside a 64x64 frame. Avoid wide robe flare or dramatic sprawl until the final frame, and even then keep the body readable as one contiguous shape.
Style: authentic 16-bit pixel art, crisp pixel clusters, stepped shading, restrained palette, production game asset, not concept art.
Constraints: transparent background, no blood, no scenery, no floor, no glow, no haze, no blur, no dramatic ragdoll distortion, no detached cloth chunks, and no interior transparency gaps.
